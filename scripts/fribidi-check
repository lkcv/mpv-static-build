#!/bin/sh

if [[ -v SKIP ]]; then
    printf "Skipping fribidi check\n"
    exit 0
fi

if [ ! -d "$BUILD/repos/fribidi" ]; then
    git clone https://github.com/fribidi/fribidi "$BUILD/repos/fribidi"
fi

cd "$BUILD/repos/fribidi"

if [[ -n "$TAG" ]]; then
    git fetch --tags --force --all
    current_tag="$(git describe --exact-match --tags 2>/dev/null || true)"

    if [[ "$current_tag" != "$TAG" ]]; then
        printf "Switching to tag $TAG\n"

        #determine if we're on an undetached branch. if we are, then we need to pull
        if ! (git checkout $TAG 2>&1 | grep -qE '(HEAD is now at|Your branch is up to date)'); then
            git -C "$BUILD/repos/fribidi" pull 2>&1
        fi

        REBUILD="fribidi"
    fi
fi

if [[ "$CLEAN_BUILD" == "yes" ]] || [[ "$REBUILD" == "fribidi" ]] || ! ( git pull 2>&1 | grep -qE '(Already up to date|You are not currently on a branch)' ); then
    "$BUILD/scripts/fribidi-build"
else
    printf "Nothing to update for fribidi\n"
fi
