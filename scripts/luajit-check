#!/bin/sh

if [[ -v SKIP ]]; then
    printf "Skipping luajit check\n"
    exit 0
fi

if [ ! -d "$BUILD/repos/luajit" ]; then
    git clone https://github.com/LuaJIT/LuaJIT "$BUILD/repos/luajit"
fi

cd "$BUILD/repos/luajit"

if [[ -n "$TAG" ]]; then
    git fetch --tags --force --all
    current_tag="$(git describe --exact-match --tags 2>/dev/null || true)"

    if [[ "$current_tag" != "$TAG" ]]; then
        printf "Switching to tag $TAG\n"

        #determine if we're on an undetached branch. if we are, then we need to pull
        if ! (git checkout $TAG 2>&1 | grep -qE '(HEAD is now at|Your branch is up to date)'); then
            git -C "$BUILD/repos/luajit" pull 2>&1
        fi

        REBUILD="luajit"
    fi
fi

if [[ "$CLEAN_BUILD" == "yes" ]] || [[ "$REBUILD" == "luajit" ]] || ! ( git pull 2>&1 | grep -qE '(Already up to date|You are not currently on a branch)' ); then
    "$BUILD/scripts/luajit-build"
else
    printf "Nothing to update for luajit\n"
fi
